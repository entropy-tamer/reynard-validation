/**
 * Test setup for validation package
 * Uses unified core test setup from reynard-testing
 */

import { setupCoreTest } from "@entropy-tamer/reynard-testing";

// Use unified core test setup
setupCoreTest();
